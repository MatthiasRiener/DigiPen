/*
function update(progress) {
    document.getElementsByClassName('triangle')[0].style.top = (document.getElementsByClassName('triangle')[0].getBoundingClientRect().y + 1) + "px";
    document.getElementsByClassName('triangle')[0].style.left = (document.getElementsByClassName('triangle')[0].getBoundingClientRect().x + 1) + "px";

}

function draw() {
    // Draw the state of the world
}

function loop(timestamp) {
    var progress = timestamp - lastRender
    update(progress)
    draw()
    lastRender = timestamp
    window.requestAnimationFrame(loop)
}
var lastRender = 0;

window.requestAnimationFrame(loop)

*/