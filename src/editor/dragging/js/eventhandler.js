// This is where all events are 

window.onload = function () {
    console.log('Document loaded.');
    // resizeCanvas();
}

// $(window).resize(function () {
//     resizeCanvas();
// });

// function resizeCanvas() {
//     let cL = $(".lower-canvas"),
//         ctxL = cL[0].getContext('2d'),
//         cU = $(".upper-canvas"),
//         ctxU = cU[0].getContext('2d'),
//         cC = $(".canvas-container");
//         // cC.css('width', $(window).width() * 0.8 + 'px')
//         // cC.css('height', $(window).width() * 0.6 + 'px')

//         // $('#c').css('width', $(window).width() * 0.8 + 'px')
//         // $('#c').css('height', $(window).width() * 0.6 + 'px')
//         // ctxL.canvas.height = $(window).width() * 0.6;
//         // ctxL.canvas.width = $(window).width() * 0.8;
//         // ctxU.canvas.height = $(window).width() * 0.6;
//         // ctxU.canvas.width = $(window).width() * 0.8;
//     $(function () {
//         // set width and height
//     });
// }